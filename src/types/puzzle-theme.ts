export type PuzzleTheme = {
  title: {
    en: string;
    vi: string;
  };
  description: {
    en: string;
    vi: string;
  };
  code: string;
  value: string;
  label: string;
  _id: string;
  priority?: number;
};
